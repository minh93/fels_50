<li>
  <%= gravatar_for user %>
  <% if current_user? user %>
    <%= link_to user.name, user_path(user) %>
  <% else %>
    <%= link_to user.name, admin_user_path(user) %>
  <% end %>
  <%= user.email %>
</li>
